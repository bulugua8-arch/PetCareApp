# ==========================================
# 3. 界面逻辑 (修改后：自动读取后台秘密，不给客户看)
# ==========================================
st.set_page_config(page_title="高级宠物档案系统", page_icon="🐾", layout="wide")

# 从后台保险柜直接拿密码
try:
    api_key = st.secrets["DEEPSEEK_API_KEY"]
    base_url = st.secrets["DEEPSEEK_BASE_URL"]
    model_name = "deepseek-chat" # 或者你常用的模型名
except:
    st.error("❌ 后台配置未完成，请联系管理员配置 API Key")
    st.stop()

# 侧边栏现在只用来放温馨提示，不放输入框了
with st.sidebar:
    st.header("🐾 欢迎使用")
    st.write("这是您的专属宠物顾问系统。")
    if not os.path.exists(FONT_PATH):
        st.error("❌ 缺字体文件")
    st.info("💡 填好右侧信息，点击生成即可。")
openai
fpdf2
